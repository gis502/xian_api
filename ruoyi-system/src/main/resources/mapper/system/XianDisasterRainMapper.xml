<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.system.mapper.XianDisasterRainMapper">
    <select id="selectAllEq" resultType="com.ruoyi.system.domain.entity.XianDisasterRain">
        SELECT
            disaster_id,
            disaster_name,
            occurrence_time,
            rainfall,
            duration,
            position,
            create_time,
            update_time,
            is_deleted,
            ST_AsText(geom) AS geom
        FROM xian_disaster_rain
        ORDER BY occur_time DESC
    </select>
    <select id="getDisasterRainById" resultType="com.ruoyi.system.domain.entity.XianDisasterRain">
        SELECT
            disaster_id,
            disaster_name,
            occurrence_time,
            rainfall,
            duration,
            position,
            create_time,
            update_time,
            is_deleted,
            ST_AsText(geom) AS geom
        FROM xian_disaster_rain
        WHERE disaster_id = CAST(#{id, jdbcType=VARCHAR} AS INTEGER)
    </select>

</mapper>


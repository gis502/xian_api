<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.system.mapper.RoadMapper">

    <select id="findIntersectingRoads" resultType="com.ruoyi.system.domain.entity.Road">
        SELECT *,ST_AsText(geom) AS geomGeom
        FROM xian_road
        WHERE ST_Intersects(geom, ST_GeomFromText(#{wktPolygon}, 4490))
           OR ST_Within(geom, ST_GeomFromText(#{wktPolygon}, 4490))
    </select>

</mapper>
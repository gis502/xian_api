<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.system.mapper.HighwayMapper">

    <select id="findIntersectingHighways" resultType="com.ruoyi.system.domain.entity.Highway">
        SELECT *,ST_AsText(ST_Intersection(geom, ST_GeomFromText(#{wktPolygon}, 4490))) AS pointGeom
        FROM xian_highway
        WHERE ST_Intersects(geom, ST_GeomFromText(#{wktPolygon}, 4490))
           OR ST_Within(geom, ST_GeomFromText(#{wktPolygon}, 4490))
    </select>

</mapper>
